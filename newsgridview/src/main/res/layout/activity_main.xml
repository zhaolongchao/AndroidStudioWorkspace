<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:id="@+id/activity_main"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.zhuoxin.newsgridview.MainActivity">
    <!--网格视图控件
    android:numColumns="auto_fit"网格视图的列数，
    值：设置数值或者auto_fit（自动）
    android:columnWidth="80dp" 设置列宽（通常这个属性结合使用）
    android:verticalSpacing="5dp"垂直间距
    android:horizontalSpacing="5dp"水平间距
    android:stretchMode="spacingWidth"缩放模式
    每一元素的内容的缩放模式
    android:listSelector="@drawable/grid_item_bg"
    设置列表每个元素的背景效果
    将某一属性值设置为null使用@null
    将GridView单项点击设置为透明色设置属性值@android:color/transparent
    android:scrollbars="none"去掉滚动条
    -->
    <GridView
        android:id="@+id/gv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:numColumns="4"
        android:columnWidth="60dp"
        android:verticalSpacing="5dp"
        android:horizontalSpacing="5dp"
        android:stretchMode="columnWidth"
        android:listSelector="@android:color/transparent"
        android:scrollbars="none"/>
</RelativeLayout>
